# Required
ANTHROPIC_API_KEY=sk-ant-...

# Optional — without it, GitHub API has 60 req/hr limit (vs 5000 with token)
# and code search is disabled. Token only needs `public_repo` scope.
GITHUB_TOKEN=

# Optional — defaults to claude-haiku-4-5
CLAUDE_MODEL=claude-haiku-4-5
